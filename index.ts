import { BehaviorManager } from "./src";
export { BehaviorManager };

// Debug log to confirm behaviors.js is loaded
if (typeof console !== 'undefined' && console.log) {
  console.log('[BROWSERTRIX-BEHAVIORS] behaviors.js loaded at', new Date().toISOString());
}
